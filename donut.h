#ifndef DONUT_H
#define DONUT_H

#include <stdio.h>
#include <math.h>
#include <string.h>
#include <windows.h>
#include <conio.h>

#define THETA_SPC 0.07 //Espacio entre cada punto que conforma a los circulos que conforman la dona
#define PHI_SPC 0.02 //Espacio entre cada circulo que conforma a la dona
#define R1 1 //Radio del circulo creado por THETA
#define R2 2 //Radio de la dona
#define K2 5 //Distancia del espectador a la dona

void hideCursor();

#endif

